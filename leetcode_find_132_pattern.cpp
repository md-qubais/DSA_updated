class Solution {
public:
    bool find132pattern(vector<int>& nums){
        stack<int> s;
        for(int i=0;i<nums.size();i++){
            if(s.empty()){
                s.push(nums[i]);
            }else{
                if(s.size()){

                }
            }
        }
        return false;
    }
};