class Solution {
public:
    bool checkPossibility(vector<int>& nums) {
        //first check from left to right
        if(nums.size()==0 or nums.size()==1){
            return true;
        }
        int idx=-1;
        for(int i=1;i<nums.size();i++){
            if(arr[i]<arr[i]){
                continue;
            }else{
                idx=
            }
        }
        if(idx==-1){
            return true;
        }
        //second check from right to left
    }
};